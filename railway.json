{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py collectstatic --noinput && gunicorn restaurant_management.wsgi:application --log-file - --bind 0.0.0.0:$PORT"
    }
}